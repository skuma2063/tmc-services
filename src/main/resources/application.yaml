server:
  port: 8081

spring:
  application:
    name: tmc-services

  datasource:
    url: jdbc:postgresql://localhost:5432/microservices
    username: admin
    password: admin123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect